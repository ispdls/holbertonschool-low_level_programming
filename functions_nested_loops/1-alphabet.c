#include <stdio.h>
/**
 * main - print _putchar
 * Return: away (0)
 */
void print_alphabet(void)
{
char ch;
for (ch = 'a' ; ch <= 'z' ; ch++)
{
putchar(ch);
return;
}
}

int main(void)
{
print_alphabert();
return (0);
}

